// const deleteDriver = async(req, res) => {
//   const { id } =  req.params
  
//   const result = await pool.query("DELETE FROM task WHERE id = $1", [id])
//   if (result.rowCount === 0) {
    
//   }
    

//   res.send("delet a driver")
// } 
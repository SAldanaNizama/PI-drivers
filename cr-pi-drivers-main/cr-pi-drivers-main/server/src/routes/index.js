const { Router } = require("express");
const router = Router();

// const driver = require("./driversRoutes")
// const teams = require("./teams")

// router.use("/", driver);  
// router.use("/", teams)

module.exports = router;
